{
  "name": "file_wizard_front_end",
  "version": "1.0.0",
  "description": "A front end interface to manipulate files using Bootstrap.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm-run-all --parallel css-watch tsc-watch",
    "css-watch": "tailwindcss -i ./src/input.css -o ./src/public/styles.css --watch",
    "tsc-watch": " tsc --watch --project tsconfig.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.0.3",
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^3.4.1"
  },
  "dependencies": {
    "typescript": "^5.3.3"
  }
}

